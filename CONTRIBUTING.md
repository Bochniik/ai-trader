# Orion Trader Development Workflow

## Before starting work

git checkout main
git pull

git checkout -b feature/my-feature

## During development

git add .
git commit -m "Describe change"

git push

## Merge after testing